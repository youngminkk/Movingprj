package kr.co.mpago.mapper;

import java.util.List;

import kr.co.mpago.domain.Notice;

public interface NoticeMapper {
    
	/*
	 * // 공지사항 목록 조회 List<Notice> getList();
	 * 
	 * // 공지사항 단일 조회 Notice get(Long bno);
	 * 
	 * // 공지사항 등록(관리자 여부) int insert(Notice notice);
	 * 
	 * // 공지사항 수정 boolean modify(Notice notice);
	 * 
	 * // 공지사항 삭제 boolean delete(Long bno);
	 */

    
}